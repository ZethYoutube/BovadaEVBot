services:
  - type: web
    name: bovada-ev-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: ENV
        value: production
      - key: LOG_LEVEL
        value: INFO
      # Set TELEGRAM_BOT_TOKEN in Render dashboard for security


